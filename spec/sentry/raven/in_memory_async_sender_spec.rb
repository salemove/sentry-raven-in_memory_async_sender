RSpec.describe Sentry::Raven::InMemoryAsyncSender do
  it "has a version number" do
    expect(Sentry::Raven::InMemoryAsyncSender::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
